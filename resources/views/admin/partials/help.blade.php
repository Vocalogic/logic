<div class="card mb-4">
    <div class="card-body">
        <div class="d-flex flex-wrap flex-column align-items-center">
            <div class="mb-3">
                <img src="/icons/{{$icon}}" class="rounded-circle img-thumbnail" style="max-height: 150px;" alt="system-helper">
            </div>
            <div class="text-center">
                <div class="fs-5">{{$title}}</div>
                <div class="small mb-3 text-muted">{{$body}}</div>
                <a href="{{$target}}" class="btn btn-primary"><i class="fa fa-plus me-2"></i>{{$action}}</a>
            </div>
        </div>
    </div>
</div>
